export type Address = {
	[type: string] : {
		building: string,
		apartment: number,
		landmark: string,
		street: string,
		province: string,
		zip_code: string,
		country: string,
		city: string
	}
}